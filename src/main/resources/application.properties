# ═══════════════════════════════════════════════════════════
#  WhiteFox Backend — Application Properties
#  Override any value via environment variables:
#    DB_URL, DB_USERNAME, DB_PASSWORD, JWT_SECRET
# ═══════════════════════════════════════════════════════════

# ── Database (PostgreSQL) ──────────────────────────────────
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/whitefox}
spring.datasource.username=${DB_USERNAME:mahimahi}
spring.datasource.password=${DB_PASSWORD:1234}

# ── JPA ───────────────────────────────────────────────────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.flyway.enabled=false

# ── JWT ───────────────────────────────────────────────────
jwt.secret=${JWT_SECRET:whitefoxSuperSecureJwtSecretKey2026@123}

# ── Server ────────────────────────────────────────────────
server.port=8080
spring.main.allow-bean-definition-overriding=true

# ── Razorpay (optional — for payment integration) ─────────
razorpay.key_id=rzp_test_TEJ88TBNljkn4Y
razorpay.key_secret=yhRbCHxM6JNsi5p2geOI5qFR

# ── Couchbase (Caching & OTP) ─────────────────────────────
spring.couchbase.connection-string=couchbases://cb.8khjpgxjip31qdb.cloud.couchbase.com
spring.couchbase.username=Dhiran
spring.couchbase.password=Whitefoxgetitdone@123
spring.data.couchbase.bucket-name=travel-sample
